#pragma once
#include <bits/stdc++.h>
using namespace std;

float car_x = -.55, car_y = 4;
int car_pos = 0;
int car_speed = 45;

GLuint sky_texture;
int width, height, nrChannels;

float obstacle_y = -3.3;

int score = 0, totalMeter = 0;
string quote;
